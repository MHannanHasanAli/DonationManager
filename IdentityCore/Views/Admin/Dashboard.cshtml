<style>
    #mainheading {
        color: white;
    }

    .cashimage {
        margin-left: 50%;
    }

    .longimage {
        margin-left: 60%;
        margin-right: 5%:
        
    }

    .white {
        color: white;
    }
    hr{
        color:white;
    }
</style>

<div class="row">
    <div class="col-sm-4">
        <div class="card">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h6 class="card-title">
                            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" class="bi bi-currency-dollar" viewBox="0 0 16 16">
                                <path d="M4 10.781c.148 1.667 1.513 2.85 3.591 3.003V15h1.043v-1.216c2.27-.179 3.678-1.438 3.678-3.3 0-1.59-.947-2.51-2.956-3.028l-.722-.187V3.467c1.122.11 1.879.714 2.07 1.616h1.47c-.166-1.6-1.54-2.748-3.54-2.875V1H7.591v1.233c-1.939.23-3.27 1.472-3.27 3.156 0 1.454.966 2.483 2.661 2.917l.61.162v4.031c-1.149-.17-1.94-.8-2.131-1.718H4zm3.391-3.836c-1.043-.263-1.6-.825-1.6-1.616 0-.944.704-1.641 1.8-1.828v3.495l-.2-.05zm1.591 1.872c1.287.323 1.852.859 1.852 1.769 0 1.097-.826 1.828-2.2 1.939V8.73l.348.086z" />
                            </svg>Cash Amount
                        </h6>
                        <div class="d-flex align-items-center">
                        <svg xmlns="http://www.w3.org/2000/svg" width="10" height="10" fill="currentColor" class="bi bi-currency-dollar" viewBox="0 0 16 16">
                            <path d="M4 10.781c.148 1.667 1.513 2.85 3.591 3.003V15h1.043v-1.216c2.27-.179 3.678-1.438 3.678-3.3 0-1.59-.947-2.51-2.956-3.028l-.722-.187V3.467c1.122.11 1.879.714 2.07 1.616h1.47c-.166-1.6-1.54-2.748-3.54-2.875V1H7.591v1.233c-1.939.23-3.27 1.472-3.27 3.156 0 1.454.966 2.483 2.661 2.917l.61.162v4.031c-1.149-.17-1.94-.8-2.131-1.718H4zm3.391-3.836c-1.043-.263-1.6-.825-1.6-1.616 0-.944.704-1.641 1.8-1.828v3.495l-.2-.05zm1.591 1.872c1.287.323 1.852.859 1.852 1.769 0 1.097-.826 1.828-2.2 1.939V8.73l.348.086z" />
                        </svg>
                        <div id="cashamount">
                        </div>
                        </div>
                    </div>
                    <img class="cashimage" src="~/images/dollar.png" style="width: 30px; height: 30px;" />
                </div>
            </div>
        </div>
    </div>


    <div class="col-sm-4">
        <div class="card">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h6 class="card-title">
                            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" class="bi bi-currency-dollar" viewBox="0 0 16 16">
                                <path d="M4 10.781c.148 1.667 1.513 2.85 3.591 3.003V15h1.043v-1.216c2.27-.179 3.678-1.438 3.678-3.3 0-1.59-.947-2.51-2.956-3.028l-.722-.187V3.467c1.122.11 1.879.714 2.07 1.616h1.47c-.166-1.6-1.54-2.748-3.54-2.875V1H7.591v1.233c-1.939.23-3.27 1.472-3.27 3.156 0 1.454.966 2.483 2.661 2.917l.61.162v4.031c-1.149-.17-1.94-.8-2.131-1.718H4zm3.391-3.836c-1.043-.263-1.6-.825-1.6-1.616 0-.944.704-1.641 1.8-1.828v3.495l-.2-.05zm1.591 1.872c1.287.323 1.852.859 1.852 1.769 0 1.097-.826 1.828-2.2 1.939V8.73l.348.086z" />
                            </svg>Bank Balance
                        </h6>
                        <div class="d-flex align-items-center">
                            <svg xmlns="http://www.w3.org/2000/svg" width="10" height="10" fill="currentColor" class="bi bi-currency-dollar" viewBox="0 0 16 16">
                                <path d="M4 10.781c.148 1.667 1.513 2.85 3.591 3.003V15h1.043v-1.216c2.27-.179 3.678-1.438 3.678-3.3 0-1.59-.947-2.51-2.956-3.028l-.722-.187V3.467c1.122.11 1.879.714 2.07 1.616h1.47c-.166-1.6-1.54-2.748-3.54-2.875V1H7.591v1.233c-1.939.23-3.27 1.472-3.27 3.156 0 1.454.966 2.483 2.661 2.917l.61.162v4.031c-1.149-.17-1.94-.8-2.131-1.718H4zm3.391-3.836c-1.043-.263-1.6-.825-1.6-1.616 0-.944.704-1.641 1.8-1.828v3.495l-.2-.05zm1.591 1.872c1.287.323 1.852.859 1.852 1.769 0 1.097-.826 1.828-2.2 1.939V8.73l.348.086z" />
                            </svg>
                            <div id="bankamount">
                            </div>
                        </div>
                    </div>
                    <img class="cashimage" src="~/images/balance.png" style="width: 30px; height: 30px;" />
                </div>
            </div>
        </div>
    </div>
    <div class="col-sm-4">
        <div class="card">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h6 class="card-title">
                            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" class="bi bi-currency-dollar" viewBox="0 0 16 16">
                                <path d="M4 10.781c.148 1.667 1.513 2.85 3.591 3.003V15h1.043v-1.216c2.27-.179 3.678-1.438 3.678-3.3 0-1.59-.947-2.51-2.956-3.028l-.722-.187V3.467c1.122.11 1.879.714 2.07 1.616h1.47c-.166-1.6-1.54-2.748-3.54-2.875V1H7.591v1.233c-1.939.23-3.27 1.472-3.27 3.156 0 1.454.966 2.483 2.661 2.917l.61.162v4.031c-1.149-.17-1.94-.8-2.131-1.718H4zm3.391-3.836c-1.043-.263-1.6-.825-1.6-1.616 0-.944.704-1.641 1.8-1.828v3.495l-.2-.05zm1.591 1.872c1.287.323 1.852.859 1.852 1.769 0 1.097-.826 1.828-2.2 1.939V8.73l.348.086z" />
                            </svg>Total Balance
                        </h6>
                        <div class="d-flex align-items-center">
                            <svg xmlns="http://www.w3.org/2000/svg" width="10" height="10" fill="currentColor" class="bi bi-currency-dollar" viewBox="0 0 16 16">
                                <path d="M4 10.781c.148 1.667 1.513 2.85 3.591 3.003V15h1.043v-1.216c2.27-.179 3.678-1.438 3.678-3.3 0-1.59-.947-2.51-2.956-3.028l-.722-.187V3.467c1.122.11 1.879.714 2.07 1.616h1.47c-.166-1.6-1.54-2.748-3.54-2.875V1H7.591v1.233c-1.939.23-3.27 1.472-3.27 3.156 0 1.454.966 2.483 2.661 2.917l.61.162v4.031c-1.149-.17-1.94-.8-2.131-1.718H4zm3.391-3.836c-1.043-.263-1.6-.825-1.6-1.616 0-.944.704-1.641 1.8-1.828v3.495l-.2-.05zm1.591 1.872c1.287.323 1.852.859 1.852 1.769 0 1.097-.826 1.828-2.2 1.939V8.73l.348.086z" />
                            </svg>
                            <div id="totalamount">
                            </div>
                        </div>
                    </div>
                    <img class="cashimage" src="~/images/dollar-symbol.png" style="width: 30px; height: 30px;" />
                </div>
            </div>
        </div>
    </div>
</div>
<br />
<div class="row">
    <div class="table_display col-md-8  text-center">
        <h4 class="text-center white">Latest Donations</h4>
        <table class="table table-bordered" id="myTable">
            <thead>
                <tr class="text-center white">
                    <th>Amount</th>
                    <th>Fees</th>
                    <th>Intention</th>

                </tr>
            </thead>
            <tbody class="text-center white" id="table_data">
            </tbody>
        </table>
        <nav id="paginationTableContainer">
            <ul id="paginationTable" class="pagination justify-content-center"></ul>
        </nav>
    </div>
    <div class="col-md-4 text-center">
        <canvas id="balanceChart" width="200" height="200"></canvas>
    </div>


</div>
<br />
<hr />
<h4 class="text-center white">Latest Projects</h4>
<div class="row" id="ProjectCardView">
</div>
<nav id="paginationContainer">
    <ul id="pagination" class="pagination justify-content-center"></ul>
</nav>

<div class="modal fade modal" id="createProjectWindow">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="text-info modal-title" id="Projectheading">Add Project</h1>
                <button type="button" class="btn-close" aria-label="Close" onclick="closeProjectModal();"></button>
                @* <button class="close text-danger" data-dismiss="modal">&times;</button>*@
            </div>
            <div class="modal-body">
                <div>
                    <form>
                        <div class="form-group">
                            <input type="hidden" id="popupId" class="form-control" disabled="disabled" autocomplete="off" />
                        </div>
                        <div class="form-group">
                            <label for="name">Name</label>
                            <input type="text" class="form-control" id="popupname" placeholder="Enter project name">
                        </div><br /><br />
                        <div class="form-group">
                            <label for="countries">Countries</label>
                            <input type="text" class="form-control" id="popupcountries" placeholder="Enter countries (comma-separated)">
                        </div><br /><br />
                        <div class="form-group">
                            <label for="estimatedTarget">Estimated Target</label>
                            <input type="date" class="form-control" id="popupestimatedTarget">
                        </div><br /><br />
                        <div class="form-group">
                            <label for="managedBy">Managed By</label>
                            <input type="text" class="form-control" id="popupmanagedBy" placeholder="Enter project manager">
                        </div><br /><br />
                        <div class="form-group">
                            <label for="teamsOnTheGround">Teams on the Ground</label>
                            <input type="number" class="form-control" id="popupteamsOnTheGround">
                        </div><br /><br />
                        <div class="form-group">
                            <label for="projectCompleted">Project Initiated</label>
                            <input type="date" class="form-control" id="popupprojectInitiated">
                        </div><br /><br />
                        <div class="form-group">
                            <label for="projectCompleted">Project Completed</label>
                            <input type="date" class="form-control" id="popupprojectCompleted">
                        </div><br /><br />
                        <div class="form-group">
                            <label for="notes">Notes</label>
                            <textarea class="form-control" id="popupnotes" rows="3" placeholder="Enter project notes"></textarea>
                        </div>
                    </form>
                </div>


            </div>
            <div class="modal-footer">
                <button class="btn btn-danger" id="CloseProjectBTN" onclick="closeProjectModal();" style="display:none">Close</button>
            </div>
        </div>
    </div>
</div>
<hr />
<h4 class="text-center white">Latest Customers</h4>
<div class="row" id="CustomerCardView">
</div>
<nav id="paginationCustomerContainer">
    <ul id="paginationCustomer" class="pagination justify-content-center"></ul>
</nav>
<div class="modal fade modal" id="createCustomerWindow">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="text-info modal-title" id="Customerheading">Add Customer</h1>
                <button type="button" class="btn-close" aria-label="Close" onclick="closeCustomerModal()"></button>
                @* <button class="close text-danger" data-dismiss="modal">&times;</button>*@
            </div>
            <div class="modal-body">
                <div>
                    <form>
                        <div class="form-group">
                            <input type="hidden" id="popupId" class="form-control" disabled="disabled" autocomplete="off" />
                        </div>
                        <div class="form-group">
                            <label>Name</label>
                            <input placeholder="Name" id="popupName" class="form-control" autocomplete="off" />
                        </div><br /><br />
                        <div class="form-group">
                            <label>Address</label>
                            <input placeholder="Address" id="popupAddress" class="form-control" autocomplete="off" />
                        </div><br /><br />
                        <div class="form-group">
                            <label>Contact No</label>
                            <input type="tel" placeholder="Phone Number" id="popupContactNo" class="form-control" autocomplete="off" />
                        </div><br /><br />
                        <div class="form-group">
                            <label>Email</label>
                            <input type="email" placeholder="E-mail" id="popupEmail" class="form-control" autocomplete="off" />
                        </div>
                    </form>
                </div>


            </div>
            <div class="modal-footer">
<button class="btn btn-danger" id="CloseCustomerBTN" onclick="closeCustomerModal();" style="display:none">Close</button>
            </div>
        </div>
    </div>
</div>

<script>
    $(document).ready(function(){
        BalanceShow();
        DonationShow();
        ProjectShow();
        CustomerShow();
    })
    function createProject() {
        cleartextbox();
        $('#createProjectWindow').modal('show');
    }
    function BalanceShow() {

            $.ajax({
            url: '/Admin/DashboardBalanceIndex',
                type: 'GET',
                dataType: 'json',
                contentType: 'application/json;charset=utf-8;',

                success: function (result) {

                var object = '';

                $.each(result, function (index, item) {
                    object += '<h4>' + item._CashBalance + '</h4>';
  
                });
                $('#cashamount').html(object);
                object = '';


                $.each(result, function (index, item) {
                    object += '<h4>' + item._BankBalance + '</h4>';


                });

                $('#bankamount').html(object);
                object = '';
                $.each(result, function (index, item) {
                    object += '<h4>' + item._TotalBalance + '</h4>';
                });
                $('#totalamount').html(object);

                $.each(result, function (index, item) {
                    var moneyGoingIn = item._MoneyGoingIn;
                    var moneyPaidOut = item._MoneyPaidOut;

                    var chartCanvas = document.getElementById('balanceChart');
                    console.log(moneyGoingIn);
                    // Create the chart
                    var chart = new Chart(chartCanvas, {
                        type: 'doughnut',
                        data: {
                            labels: ['Money Going In', 'Money Paid Out'],
                            datasets: [{
                                data: [moneyGoingIn, moneyPaidOut],
                                backgroundColor: ['#36A2EB', '#FF6384'],
                                borderWidth: 0
                            }]
                        },
                        options: {
                            responsive: true,
                            maintainAspectRatio: false,
                            cutoutPercentage: 70, // Adjust the value to change the size of the hole in the center
                            plugins: {
                                legend: {
                                    display: true,
                                    position: 'bottom', // Set the legend position to bottom
                                    labels: {
                                        color: 'white' // Set the legend text color to white
                                    },
                                    title: {
                                        display: true,
                                        color: 'white', // Set the legend title color to white
                                        font: {
                                            size: 12 // Set the font size of the legend title
                                        }
                                    }
                                }
                            }
                        }
                    });
                
               
                });
               
            },
            error: function () {
            
            }
           
            });
        
    }
    function ProjectShow() {

        $.ajax({
            url: '/Admin/DashboardProjectIndex',
            type: 'GET',
            dataType: 'json',
            contentType: 'application/json;charset=utf-8;',

            success: function (result) {
                var object = '';
                var count = 0;

                $.each(result, function (index, item) {
                    if (count >= 10) {

                        return false;
                    }
                    object += '<div class="col-4">';
                    object += '<div class="card text-center cardPagination"';
                    object += '<div class="card-body text-center">';
                    object += '<h2 class="card-title">' + item._Name + '</h2>';
                    object += '<p class="card-text">' + item._ManagedBy + '</p>';
                    object += '<a href="#" class="btn btn-primary" onclick="viewProject(' + item._id + ')"> + Details</a>';
                    object += '</div>';
                    object += '</div>';
                    object += '</div>';
                    count++;
                });
                $('#ProjectCardView').html(object);
                if (count > 3) {
                    Projectpagination();
                }
                var container = $('#ProjectCardView');
                var pagination = $('#pagination');
                container.css('margin-bottom', '20px');
                pagination.css('margin-top', '20px');
            },
            error: function () {

            }
        });
    }
    function cleartextbox() {

        $('#popupname').val('');
        $('#popupcountries').val('');
        $('#popupestimatedTarget').val('');
        $('#popupmanagedBy').val('');
        $('#popupteamsOnTheGround').val('');
        $('#popupprojectInitiated').val('');
        $('#popupprojectCompleted').val('');
        $('#popupnotes').val('');

    }
    function viewProject(id) {
        $.ajax({
            url: '/Admin/ProjectEdit?id=' + id,
            type: 'GET',
            success: function (response) {
                createProject();

                var f1 = response._EstimatedTarget;
                var f2 = response._ProjectInitiated;
                var f3 = response._ProjectCompleted;

                var f4 = f1.split('T')[0];
                var f5 = f2.split('T')[0];
                var f6 = f3 !== null ? f3.split('T')[0] : null;



                $('#popupId').val(response._id);
                $('#popupname').val(response._Name);
                $('#popupcountries').val(response._Countries);
                $('#popupestimatedTarget').val(f4);
                $('#popupmanagedBy').val(response._ManagedBy);
                $('#popupteamsOnTheGround').val(response._TeamsOnTheGround);
                $('#popupprojectInitiated').val(f5);
                $('#popupprojectCompleted').val(f6);
                $('#popupnotes').val(response._Notes);
                //can work with both css and hide function---same result
                //$('#AddProjectBTN').css('display','none');
                //$('#UpdateProjectBTN').css('display', 'block');

                $('#AddProjectBTN').hide();
                $('#UpdateProjectBTN').hide();
                $('#CloseProjectBTN').show();
                $('#Projectheading').text('Project Information');
                ProjectShow();
            },
            error: function () {

            }
        });
    }
    function closeProjectModal() {
        $('#createProjectWindow').modal('hide');
    }
    function Projectpagination() {

        var cards = $('.cardPagination');
        var cardsPerPage = 3; // Number of cards to show per page

        var totalCards = cards.length;
        if (totalCards <= cardsPerPage) {
            // No need for pagination if the total cards do not exceed the limit
            return;
        }

        var totalPages = Math.ceil(totalCards / cardsPerPage);

        // Clear existing pagination links
        $('#pagination').empty();

        for (var i = 1; i <= totalPages; i++) {
            $('#pagination').append('<li class="page-item"><a class="page-link" href="#">' + i + '</a></li>');
        }

        // Show the first page by default
        showPage(1);

        // Handle click event on pagination links
        $('#pagination').on('click', 'li.page-item', function () {
            event.preventDefault();
            var pageNumber = $(this).text();
            showPage(pageNumber);
        });

        function showPage(pageNumber) {
            var startIndex = (pageNumber - 1) * cardsPerPage;
            var endIndex = startIndex + cardsPerPage;
            cards.hide().slice(startIndex, endIndex).show();

            // Update active state of pagination links
            $('#pagination li.page-item').removeClass('active');
            $('#pagination li.page-item:eq(' + (pageNumber - 1) + ')').addClass('active');
        }
    }
    
    //Index Donation
    function DonationShow() {

        $.ajax({
            url: '/Admin/DashboardDonationIndex',
            type: 'GET',
            dataType: 'json',
            contentType: 'application/json;charset=utf-8;',
            success: function (result) {
                var object = '';
                var count = 0;
                $.each(result, function (index, item) {
                    if (count >= 10) {
                        
                        return false;
                    }
                    object += '<tr>';
                    object += '<td> $' + item._Amount + '</td>';
                    object += '<td> $' + item._Fees + '</td>';
                    object += '<td>' + item._Intention + '</td>';
                    //object += '<td><a href="#" id="IconButtonEdit" class="btn btn-primary fa-solid" onclick="editDonation(' + item._id + ')"><i class="fa fa-pen" aria-hidden="true"></i></a><a href="#"  id="IconButtonDel" class="btn btn-danger fa-solid" onclick="deleteDonation(' + item._id + ' )"><i class="fa fa-trash" aria-hidden="true"></i></a></td>';
                    object += '</tr>';
                    count++;
                });
                $('#table_data').html(object);
                if (count > 5) {
                    pagination();
                }



            },
            error: function () {

            }

        });

    }
    //Pagination
    function pagination() {
        var table = $('#myTable');
        var rowsPerPage = 4; // Number of rows to show per page

        var totalRows = table.find('tbody tr').length;
        if (totalRows <= rowsPerPage) {
            // No need for pagination if the total rows do not exceed the limit
            return;
        }

        var totalPages = Math.ceil(totalRows / rowsPerPage);

        // Clear existing pagination links
        $('#paginationTable').empty();

        for (var i = 1; i <= totalPages; i++) {
            $('#paginationTable').append('<li class="page-item"><a class="page-link" href="#">' + i + '</a></li>');
        }

        // Show the first page by default
        showPage(1);

        // Handle click event on pagination links
        $('#paginationTable').on('click', 'li.page-item', function () {
            var pageNumber = $(this).text();
            showPage(pageNumber);
        });

        function showPage(pageNumber) {
            var startIndex = (pageNumber - 1) * rowsPerPage;
            var endIndex = startIndex + rowsPerPage;
            table.find('tbody tr').hide().slice(startIndex, endIndex).show();

            // Update active state of pagination links
            $('#paginationTable li.page-item').removeClass('active');
            $('#paginationTable li.page-item:eq(' + (pageNumber - 1) + ')').addClass('active');
        }
    }

    function createCustomer() {
        clearCustomertextbox();
        $('#createCustomerWindow').modal('show');
    }
    function closeCustomerModal() {
        $('#createCustomerWindow').modal('hide');
    }
    function clearCustomertextbox() {
        //$('#popupId').val('');
        $('#popupName').val('');
        $('#popupAddress').val('');
        $('#popupContactNo').val('');
        $('#popupEmail').val('');
    }
    function CustomerShow() {
        $.ajax({
            url: '/Admin/DashboardCustomerIndex',
            type: 'GET',
            dataType: 'json',
            contentType: 'application/json;charset=utf-8;',
            success: function (result) {
                var object = '';
                var count = 0;
                $.each(result, function (index, item) {
                    object += '<div class="col-sm-3">';
                    object += '<div class="card cardCustomer">';
                    object += '<div class="card-body">';
                    object += '<div class="d-flex align-items-center">';
                    object += '<img src="/images/man.png" style="width: 30px; height: 30px;" />';
                    object += '<h5 class="card-title">' + item._Name + '</h5>';
                    object += '</div>';
                    object += '<p class="card-text">' + item._Email + '</p>';
                    object += '<a href="#" class="btn btn-primary" onclick="viewCustomer(' + item._id + ')"> + Details</a>';
                    object += '</div>';
                    object += '</div>';
                    object += '</div>';
                    count++;
                });
                $('#CustomerCardView').html(object);
                if (count > 4) {
                    Customerpagination();
                }
                var paginationContainer = $('#paginationCustomerContainer');
                paginationContainer.css('margin-top', '20px');
            },
            error: function () {
                // Handle error
            }
        });
    }

    function viewCustomer(id) {
        $.ajax({
            url: '/Admin/CustomerEdit?id=' + id,
            type: 'GET',
            success: function (response) {
                createCustomer();
                $('#popupId').val(response._id);
                $('#popupName').val(response._Name);
                $('#popupAddress').val(response._Address);
                $('#popupContactNo').val(response._PhoneNumber);
                $('#popupEmail').val(response._Email);
                //can work with both css and hide function---same result
                //$('#AddCustomerBTN').css('display','none');
                //$('#UpdateCustomerBTN').css('display', 'block');


                $('#CloseCustomerBTN').show();
                $('#Customerheading').text('Customer Information');
                CustomerShow();
            },
            error: function () {
                // Handle error
            }
        });
    }

    // Pagination
    function Customerpagination() {
        var cards = $('.cardCustomer');
        var cardsPerPage = 4; // Number of cards to show per page

        var totalCards = cards.length;
        if (totalCards <= cardsPerPage) {
            // No need for pagination if the total cards do not exceed the limit
            return;
        }

        var totalPages = Math.ceil(totalCards / cardsPerPage);

        // Clear existing pagination links
        $('#paginationCustomer').empty();

        for (var i = 1; i <= totalPages; i++) {
            $('#paginationCustomer').append('<li class="page-item"><a class="page-link" href="#">' + i + '</a></li>');
        }

        // Show the first page by default
        showPage(1);

        // Handle click event on pagination links
        $('#paginationCustomer').on('click', 'li.page-item', function (event) {
            event.preventDefault();
            var pageNumber = $(this).text();
            showPage(pageNumber);
        });

        function showPage(pageNumber) {
            var startIndex = (pageNumber - 1) * cardsPerPage;
            var endIndex = startIndex + cardsPerPage;
            cards.hide().slice(startIndex, endIndex).show();

            // Update active state of pagination links
            $('#paginationCustomer li.page-item').removeClass('active');
            $('#paginationCustomer li.page-item:eq(' + (pageNumber - 1) + ')').addClass('active');

            // Scroll to the top of the customer cards
            $('html, body').animate({
                scrollTop: $('#CustomerCardView').offset().top
            }, 500);
        }
    }
</script>
